[build]
  publish = "public"
  command = "yarn build"
  context = "production"
  # functions = "functions"
[build.environment]
  # YARN_VERSION = "1.9.4"
  YARN_VERSION = "1.15.2"
  YARN_FLAGS = "--no-ignore-optional"
