[build]
  publish = "public"
  command = "yarn build"
  context = "production"
  # functions = "functions"
[build.environment]
  # YARN_VERSION = "1.9.4"
  YARN_VERSION = "1.19.1"
  YARN_FLAGS = "--no-ignore-optional"
[dev]
  command = "yarn develop:app"
